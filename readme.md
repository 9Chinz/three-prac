# Penalty football game project

## how to run

- install dependency

```shell
npm i
```

- install parcel
```shell
npm i parcel -g
```

- run dev
```shell
npm run dev
```

- run build to dist folder for deploy
```shell
npm run build
```